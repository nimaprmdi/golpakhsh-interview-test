// const AuthContext = createContext(defaultProvider);

// type Props = {
//     children: ReactNode;
// };

// const AuthProvider = ({ children }: Props) => {

//     const values = {};

//     return <AuthContext.Provider value={ values }> { children } </AuthContext.Provider>;
// };

// export { AuthContext, AuthProvider };

// const useAuth = (): any => {
//     const formBuilderContext = useContext(AuthContext);
//     if (!formBuilderContext) throw new Error('Auth must be used in AuthProvide

const x = () => {};

export { x };
